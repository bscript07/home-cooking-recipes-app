<h2>Recipe Catalog</h2>

<section class="recipes">
    {{#if recipes.length}}
    {{#each recipes}}
    <article class="recipe">
        <img src="{{image}}" alt="Spaghetti Carbonara">
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/recipes/{{_id}}/details">Read More</a>
    </article>
    {{/each}}
    {{else}}
    <p class="no-post">There are no recipes found yet!</p>
    {{/if}}
</section>